pseudo-elements
These are special elements and are prepended by double colons (::)
    ::after
    ::before
    ::selection
    ::first-letter
    ::first-sentence

psuedo-classes
These are a special state of the elements
    :active
    :hover
    :focus
    :visited
