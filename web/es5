--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
Objects

newObject = Object.create(parentObject)
Object.preventExtensions(object)
Object.isExtensible(object)
    New properties cannot be added to the object, while old ones can be edited or configured 

Object.seal(object)
Object.isSealed(object)
    New properties cannot be added, neither can the properties be configures. They
    can only be edited.

Object.freeze(object)
Object.isFrozen(object)
    The object is immutable.

Object.getPrototypeOf(object)
someObject.isPrototypeOf(someOtherObject)

Object.keys(object)
    Returns all the enumerable keys of an object

Object.getOwnPropertyNames(object)
    Returns an array of all the property names that belong to that object.

Object.defineProperty(object, propertyName, descriptor)
Object.defineProperties(object, descriptors)
    A property descriptor contains the following attributes:
        value
        get
        set
        enumerable
        configurable
        writtable
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
Array extras

The following are the new array methods introduced in ES5:

    array.map();
    array.reduce();
    array.reduceRight();
    array.every();
    array.some();
    array.filter();
    array.forEach();
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
Date

    Date.now();
    Date.toISOString();
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
JSON

JSON support has landed in the browser, the following functions are supported:

    JSON.parse(string)
    JSON.parse(string, translate)
    JSON.stringify(obj)
    JSON.stringify(obj, translate)
    JSON.stringify(obj, whitelist)

translate function accepts a key and a value and returns the value for the key
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
Generics


--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
Strict mode
    1. Implicit globals raises an excpetion.
    2. Defining a property in an object literal more than once raises an
    exception.
    3. writing to an element with writable set to false will result in an error,
    and same is the case with configurable and frozen objects
    4. Assigning eval will result in an error.
    5. with statment is now deprecated
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
Arguments:
arguments in ES5 is more array like in the sense, that all the newly added
functions of ES5 for arrays can be used with arguments. Also most of the older
methods of arrays can now be used with arguments.
arguments.callee is not used
arguments is not linked with the parameter list.

Object and Array:
Changing the Object and Array constructor does not cause any effect to {} or []
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
this in inner function:
this in the inner function now points to null instead of the global object
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
Trailing commas:
trailing commas are now ignored
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
Multiline strings:
JavaScript now supports bash like multi line comments
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
property names:
Property names need not be quoted in ES5.
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
String.trim()
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
